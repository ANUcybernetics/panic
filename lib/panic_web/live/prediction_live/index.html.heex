<.header>
  <%= @network.name %> predictions
</.header>

<.prediction_grid
  :for={
    {genesis, group_predictions} <-
      group_by_run(@predictions)
  }
  class="mt-8"
  genesis={genesis}
  predictions={group_predictions}
/>

<.back navigate={~p"/networks/#{@network}"}>Back to network</.back>
